{
  "name": "ubridge-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "UBridge: secure realtime network runtime for messaging, voice, files, presence and sync.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "clean": "find packages -name dist -type d -prune -exec rm -rf {} +",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
